cmake_minimum_required(VERSION 3.20)
project(userspace_drivers LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

option(BUILD_STATIC_BINARIES "Build additional fully static exercise binaries" OFF)

function(add_exercise name)
    add_executable(${name} ${name}.cpp)
    if(BUILD_STATIC_BINARIES)
        add_executable(${name}_static ${name}.cpp)
        target_link_options(${name}_static PRIVATE
            -static
            -static-libstdc++
            -static-libgcc)
    endif()
endfunction()

add_exercise(01_pci_enum)
add_exercise(02_config_space)
add_exercise(03_mmio)
add_exercise(04_bind_unbind)
add_exercise(05_sriov_vfs)
add_exercise(06_vfio_info)
add_exercise(07_vfio_region_dump)
add_exercise(08_vfio_dma_map)
add_exercise(09_vfio_irq_eventfd)
add_exercise(10_virtio_vfio_pci_caps)
add_exercise(11_virtio_vfio_common_cfg)
add_exercise(12_virtio_vfio_queue_info)
add_exercise(13_virtio_vfio_reset_status)
add_exercise(14_virtio_vfio_feature_bits)
add_exercise(15_virtio_vfio_vring_map)
add_exercise(16_virtio_vfio_queue_program)
add_exercise(17_virtio_vfio_queue_enable)
add_exercise(18_virtio_vfio_notify_info)
add_exercise(19_virtio_vfio_notify_write)
add_exercise(20_virtio_vfio_driver_ok)
add_exercise(21_virtio_net_rx_buffers)
add_exercise(22_virtio_net_rx_observe)
add_exercise(23_virtio_net_tx_packet)
add_exercise(24_virtio_net_rx_tx_echo)
add_exercise(25_virtio_net_echo_loop)
